version: '3.8'
services:
    reason-apps-php:
        container_name: reason-apps-php
        build:
            context: ./
            dockerfile: images/php/Dockerfile
        depends_on:
            - mysql
        links:
            - mysql
        ports:
            - "8080:80"
        networks:
            - reason-network
        volumes:
            - ./config/apache/default.conf:/etc/apache2/sites-enabled/default.conf
            - type: bind
              source: ./src
              target: /var/www/html
    mysql:
        container_name: mysql
        platform: linux/x86_64
        image: mysql
        restart: always
        environment:
            MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
        ports:
            - "3306:3306"
        networks:
            - reason-network
        volumes:
            - ./storage/mysql:/var/lib/mysql
    reason-phpmyadmin:
        container_name: reason-phpmyadmin
        image: phpmyadmin
        ports:
            - "3305:80"
        restart: always
        environment:
            PMA_HOST: mysql
            PMA_PASSWORD: ""
        depends_on:
            - mysql
        networks:
            - reason-network
volumes:
    src:
networks:
  reason-network:
    external: true
